services:
  frontend:
    container_name: frontend
    build: ./frontend
    restart: unless-stopped
    ports:
      - "5173:5173"

  backend:
    container_name: backend
    build: ./backend
    restart: unless-stopped
    ports:
      - "5000:5000"